import {useState} from 'react'
import {Navigate,useNavigate} from 'react-router-dom'
import {GraduationCap} from 'lucide-react'
import {useAuth} from '../context/AuthContext'
import {Alert} from '../components/UI'
import {errorMessage} from '../services/api'
export default function Login(){const {user,login}=useAuth(),navigate=useNavigate();const [form,setForm]=useState({email:'admin@sms.local',password:'password'}),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to={`/${user.role.toLowerCase()}`}/>;const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const u=await login(form.email,form.password);navigate(`/${u.role.toLowerCase()}`)}catch(e){setError(errorMessage(e))}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-brand-900 via-slate-900 to-brand-600 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><div className="mb-8"><div className="mb-4 grid h-12 w-12 place-items-center rounded-xl bg-brand-600 text-white"><GraduationCap/></div><h1 className="text-2xl font-bold">Welcome back</h1><p className="mt-1 text-sm text-slate-500">Sign in to your campus workspace</p></div><Alert message={error}/><label className="label">Email</label><input className="field mb-4" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><label className="label">Password</label><input className="field mb-6" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button disabled={busy} className="btn w-full">{busy?'Signing in...':'Sign in'}</button><p className="mt-6 text-xs text-slate-400">Demo: admin@sms.local / password</p></form></main>}
