import {useEffect,useState} from 'react'
import api,{errorMessage} from '../services/api'
import {Alert,Empty,PageTitle} from '../components/UI'
export default function Catalog(){const [departments,setDepartments]=useState([]),[courses,setCourses]=useState([]),[faculty,setFaculty]=useState([]),[dept,setDept]=useState({code:'',name:''}),[course,setCourse]=useState({code:'',name:'',credits:4,semester:1,departmentId:'',facultyId:''}),[error,setError]=useState('');const load=()=>Promise.all([api.get('/admin/departments'),api.get('/admin/courses'),api.get('/admin/faculty')]).then(([d,c,f])=>{setDepartments(d.data);setCourses(c.data);setFaculty(f.data)});useEffect(()=>{load()},[]);const createDept=async e=>{e.preventDefault();try{await api.post('/admin/departments',dept);setDept({code:'',name:''});load()}catch(e){setError(errorMessage(e))}};const createCourse=async e=>{e.preventDefault();try{await api.post('/admin/courses',{...course,credits:+course.credits,semester:+course.semester,departmentId:+course.departmentId,facultyId:course.facultyId?+course.facultyId:null});setCourse({...course,code:'',name:''});load()}catch(e){setError(errorMessage(e))}};return <><PageTitle title="Academic catalog" subtitle="Departments, courses, and faculty assignments"/><Alert message={error}/><div className="grid gap-5 xl:grid-cols-2"><section className="card"><h2 className="mb-4 font-bold">Departments</h2><form onSubmit={createDept} className="mb-5 flex gap-2"><input className="field w-28" placeholder="Code" value={dept.code} onChange={e=>setDept({...dept,code:e.target.value})} required/><input className="field" placeholder="Department name" value={dept.name} onChange={e=>setDept({...dept,name:e.target.value})} required/><button className="btn">Add</button></form>{departments.map(d=><div className="border-t py-3 text-sm" key={d.id}><b>{d.code}</b><span className="ml-3 text-slate-500">{d.name}</span></div>)}</section><section className="card"><h2 className="mb-4 font-bold">Create course</h2><form onSubmit={createCourse} className="grid gap-3 sm:grid-cols-2"><input className="field" placeholder="Course code" value={course.code} onChange={e=>setCourse({...course,code:e.target.value})} required/><input className="field" placeholder="Course name" value={course.name} onChange={e=>setCourse({...course,name:e.target.value})} required/><input className="field" type="number" min="1" max="10" value={course.credits} onChange={e=>setCourse({...course,credits:e.target.value})}/><input className="field" type="number" min="1" max="12" value={course.semester} onChange={e=>setCourse({...course,semester:e.target.value})}/><select className="field" value={course.departmentId} onChange={e=>setCourse({...course,departmentId:e.target.value})} required><option value="">Department</option>{departments.map(d=><option key={d.id} value={d.id}>{d.code}</option>)}</select><select className="field" value={course.facultyId} onChange={e=>setCourse({...course,facultyId:e.target.value})}><option value="">Unassigned</option>{faculty.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><button className="btn">Create course</button></form></section></div><section className="mt-5 table-wrap">{courses.length===0?<Empty/>:<table><thead><tr><th>Code</th><th>Course</th><th>Credits</th><th>Semester</th><th>Faculty</th></tr></thead><tbody>{courses.map(c=><tr key={c.id}><td className="font-semibold">{c.code}</td><td>{c.name}</td><td>{c.credits}</td><td>{c.semester}</td><td>{c.faculty?.name||'Unassigned'}</td></tr>)}</tbody></table>}</section></>}
